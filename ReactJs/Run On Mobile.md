need to connect on same network
after `npm run starts`
```bash
# on browser
http://localhost:3000
``` 
```bash
# get ip information from CLI
ipconfig
```   
```bash
# on mobile browser
http://x.x.x.x:3000
```  